{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "app:android": "npm run api:start && lerna run android --scope sunshine --parallel start",
    "app:ios": "spm run api:start && lerna run ios --scope sunshine --parallel start",
    "api:start": "lerna run start --scope sunshineapi",
    "dev": "lerna run --parallel dev",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
